import { Comparator } from "@infoloop-opensource/abstractions";
export declare const identity: <S>(s: S) => S;
export declare const compareNumber: Comparator<number>;
export declare const compareNumberPair: Comparator<[number, number]>;
